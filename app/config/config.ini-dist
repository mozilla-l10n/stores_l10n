[config]

; Public path where the app is installed, if not installed at the server root
url='http://localhost/stores_l10n/web/'

; Github secret key for continuous integration on Beta site
github_key=putyoursecretkeyhere

; Set up a debug parameter for logging in dev mode to the console
debug=false
