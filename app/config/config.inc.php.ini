<?php

/*
    Path to a local clone of https://github.com/mozilla-l10n/appstores/
*/
$l10n_path = '/path/to/l10n/clone/';

/*
    This is used to include assets in templates and parse API requests.
    Change this setting according to your system: if the web app is served from
    the root, use '/', otherwise put the URL of the folder containing the
    web app, e.g. '/stores_l10n/'
*/
$webroot_folder = '/stores_l10n/';

// Set up a debug parameter for logging in dev mode to the console
const DEBUG = false;

// GitHub secret key for webhook
const GITHUB_SECRET = '';
